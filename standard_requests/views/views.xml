<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="standard_requests.list">
      <field name="name">standard_requests list</field>
      <field name="model">standard_requests.standard_requests</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="standard_requests.action_window">
      <field name="name">standard_requests window</field>
      <field name="res_model">standard_requests.standard_requests</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="standard_requests.action_server">
      <field name="name">standard_requests server</field>
      <field name="model_id" ref="model_standard_requests_standard_requests"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="standard_requests" id="standard_requests.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="standard_requests.menu_1" parent="standard_requests.menu_root"/>
    <menuitem name="Menu 2" id="standard_requests.menu_2" parent="standard_requests.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="standard_requests.menu_1_list" parent="standard_requests.menu_1"
              action="standard_requests.action_window"/>
    <menuitem name="Server to list" id="standard_requests" parent="standard_requests.menu_2"
              action="standard_requests.action_server"/>
    -->
  </data>
</odoo>